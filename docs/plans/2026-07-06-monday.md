# Monday 2026-07-06 — deploy + verification plan

Written 2026-07-03 (market closed for July 4th). Three behavior changes go live Monday: a **working IV-rank gate** (first time since v1), the **margin DTE cap** finally enforced, and the **route-chase redesign** (bid rests / mid concedes 3 ticks / ask starts at mid) — plus order-safety fixes, the startup config log, and step 1 of the liquidity-data rollout (log-only). This doc is the runbook: deploy, watch, verify, then decide the tuning questions with real data.

## 1. Deploy (before 6:30 AM PT)

- [ ] Server: `git fetch --tags && git checkout monday-2026-07-06 && npm install && npm run build` — **pin to the tag, not main's tip**: everything above the tag is behavior-changing strategy work that needs Monday's data first (Tuesday+). The tag carries the full verified Monday set.
- [ ] pm2: **`pm2 delete` + start from `ecosystem.config.cjs`** — not a plain `restart`; `restart_delay: 5000` was added 07-02 and pm2 only picks up config changes on delete+start.
- [ ] Confirm the server `.env` uses the current `CORE_*` / `STRATEGY_*` names (the deployed code reads only those; the July-1 prefix refactor renamed ~30 vars). Fast check that also proves creds + the IV fix in one shot:
  ```bash
  node --import tsx src/tools/probe-iv-rank.ts MARA SPY
  # want: "auth OK", two entries under the comma-STRING form, a scale verdict,
  # and non-null values under "what the bot actually sees today"
  ```
- [ ] Env vars recommended during the 07-02 session (set if desired, all optional):
  - `STRATEGY_MARGIN_DIP_TARGET_BOOST_MAX_PCT=0.25` (dip boost; unset = off)
  - `STRATEGY_CASH_MAX_BUY_EXPOSURE_PCT=0.10` (the 0.05 default ≈ $99/action blocked every cash seed on 07-02)
  - `STRATEGY_MAX_ALLOCATION_BUY_POSITION_MULTIPLE=3` (caps each add at 3× current position value)

## 2. Behavior changes going live

1. **IV entry gate is alive.** `getUnderlyingIvMetrics` returned `null` on every call since v1 (the SDK sent `symbols[]=X`, which the API rejects with a bare 400; fixed + scale-normalized 0–1 → 0–100 on 07-03). From Monday: candidates with rank < `STRATEGY_MIN_IV_RANK_PCT` (20) are skipped, and `skippedByIvGate` also suppresses the held-contract fallback for that symbol. Reference points from 07-03: MARA 35.5 (passes), SPY 33.0 (passes), **LCID 15.1 (blocked — the 07-02 churn symbol)**.
   - **Emergency off-switch** if the gate blocks too much: `STRATEGY_MIN_IV_RANK_PCT=0` + restart.
2. **Seed IV fallback (50 early / 70 deep) is reachable** when boolean data is dark. Note MARA at 35 would *not* qualify — the bars may be unrealistically high (tuning question #4).
3. **Liquidity fields are logged, not acted on.** Every `manage-allocation-candidate` line now carries `dayVolume`, `openInterest`, `bidSize`, `askSize`, `spreadPct`, and `top-candidate-considered` lines carry the alternatives. No gating until step 2 (section 4).
4. **Margin DTE cap is finally enforced on group targets** (fixed 07-03) — margin buys should now target ≤ `STRATEGY_MARGIN_MAX_TARGET_DTE` (7) all day instead of the 15–30 DTE blend produced by the dropped-accountType bug.
5. **Route-chase redesign** (07-03) — bid rests at the bid (no longer chases up), mid concedes at most 3 ticks, ask starts at the midpoint and fast-chases (15s) to the ask. **Expect visibly slower/quieter morning accumulation**: morning route weights are bid-heavy (0.70 at open), and a resting bid waits for a hit instead of walking up to the ask. This is correct behavior but a real character change — watch fills per route in the logs, and don't mistake "quiet morning" for the IV gate alone.
6. **Startup config log** — first lines after boot include `{"scope":"startup-config",...}` with masked secrets, the resolved key limits, and loud `[config] OBSOLETE env var …` warnings for any stale names in the server `.env`. Read this before anything else Monday morning.

## 3. Verify in Monday's data

Pull with `scripts/pull-today.sh` → `data-pull/2026-07-06/`. Gotcha: pm2 log-line prefixes are Pacific; JSON `timestamp` fields inside lines and run history are UTC.

1. **Restart storm gone** — `grep -c 'Exiting for PM2 restart'` in the error log: was 38 on 07-02; expect ~0–3, each with the backoff text "in Ns (M restarts in last 30 min)".
2. **Warm cache after restarts** — `[secret] rehydrated N cached positions from disk` present; `[secret] cached source positions: 0` rare/absent.
3. **"Target exposure is zero" collapses** — was 84/108 cash and 42/121 margin plan-diagnostic skips on 07-02, attributed to the crash loop blanking the secret cache. Count Monday's `plan.diagnostics`. Small residual is expected only for feed-untracked symbols, >10-min-stale cache on a restart, or real feed outages. **If still high with restarts gone and cache warm → escalate the base-tier decision (IMPROVEMENTS.v4 strategy #1).**
4. **ivRank live** — real numbers in seed-decision/candidate logs; possibly "IV rank X below minimum 20" skips; zero `[market-metrics] ... 400` errors. If ivRank is null everywhere, the deploy didn't include the fix.
5. **Dip boost active** (if env set) — `margin-position-gate` lines show nonzero `dipTargetBoostPct` when a group is down >2% ask with booleans ≥ 4.
6. **Honest skip messages** — buying-power skips name the limiting factor ("capped at X by per-action max buy pct / exposure headroom / remaining buying power").
7. **Held-contract fallbacks firing** — scopes `run-cycle-cash-from-margin-held-fallback` (now also on cost-blocked skips) and `manage-allocation-held-contract-fallback`.
8. **Error log small** — was 11MB of pretty-printed option chains on 07-02.
9. **Liquidity data flowing** — `bidSize`/`askSize` should be real integers during market hours (they stream as `NaN` when closed; if still null on live quotes, the size extraction in `market-data.ts` needs another field-name variant).
10. **Margin DTE ≤ 7 everywhere** — margin plan rows and fills should show target/candidate DTE ≤ 7 all day (was 15–30). Any margin row above 7 DTE means the accountType fix didn't deploy or another path still drops it.
11. **Startup config clean** — the boot `startup-config` line shows expected values and **zero** `[config] OBSOLETE` warnings; any warning = a server `.env` name to migrate on the spot. Also expect a loud `[config] TIMEZONE` warning only if the box isn't `America/Los_Angeles` (should be silent).
12. **Route pricing behaves** — per-order logs: bid-route orders sit at the bid and don't walk up; ask-route orders start at the midpoint (not the full ask) and step up on the 15s clock. No route should show the full ask as its *first* price unless the spread was already tight.

## 4. Liquidity design (full text: IMPROVEMENTS.v4 strategy #4)

Roles — keep them distinct:

| Signal | Question it answers | Weighting |
|---|---|---|
| delta | what will move (exposure selector) | unchanged — already drives margin strike targeting |
| day volume | can we exit at fair prices *today* | heaviest for margin (must exit by 12:55) |
| open interest | does a standing market exist | heavier for cash (overnight holds); stable intraday → safe to floor-gate |
| bid/ask sizes | is the quoted spread real | execution tactics + phantom-quote defense |

Rollout — one step at a time so each change stays attributable:

1. **Log-only (live Monday).** Two collection points:
   ```bash
   # chosen contract per group per cycle — incl. spread + sizes (quote-fetched)
   grep '"scope":"manage-allocation-candidate"' <out.log> | jq '{dayVolume, openInterest, bidSize, askSize, spreadPct}'
   # the considered set (top 6 sorted candidates) — vol/OI/ivx/delta from the chain
   # sample; no spread/sizes for alternatives (would cost per-candidate quote fetches)
   grep '"scope":"top-candidate-considered"' <out.log> | jq '.candidates[] | {sym, dte, vol, oi}'
   ```
   The chosen-candidate stream sets the step-2 floor thresholds (we gate what we'd buy); the considered-set stream is what the step-3 *scoring* design needs, since scoring discriminates between alternatives.
2. **Floor gates on new entries** — `STRATEGY_MIN_OPEN_INTEREST` (start ~100, set from step-1 data; ships with `.env.example` + README entries) and a phantom-quote guard: sizeless quote while the market is open ⇒ don't trust this cycle's spread-gate pass.
3. **Scoring + tactics** — replace the decorative `meetsVolumeRequirement` flag and last-place volume tiebreak with a liquidity score among spread-passing candidates (log-scaled volume + OI; volume-weighted for margin, OI-weighted for cash); teach the tick-chaser to respect sizes (don't chase an ask showing less size than the order; prefer patient mid on thin bids for closes).

Hard rule for every gate: sampling covers only ~ATM strikes in the target DTE window — **unknown degrades gracefully (pass + log), never treated as zero.** Otherwise we recreate the IV-gate silent-blocking bug in a new costume.

## 5. Open tuning questions (answer with Monday+ data)

1. Does "strongest booleans at the bottom" repeat (07-02 MARA: scores 9–10 at the −21% bid low)?
2. Does cash actually seed + accumulate through a dip end-to-end now that all three 07-02 blockers are fixed?
3. Is the ≥4 boolean bar for the dip boost right, or should it be ≥7?
4. Are the seed IV-fallback bars (50/70) reachable given real rank distributions (MARA 35.5, SPY 33.0 on 07-03)?
5. Zero-target residual after the crash-loop fix: noise, or does it justify the unsignalled base tier (v4 strategy #1)?
6. If `STRATEGY_MAX_ALLOCATION_BUY_POSITION_MULTIPLE=3` is set: how often do positions rip in the first ~20 min without dipping (the only case the multiple costs money)?
